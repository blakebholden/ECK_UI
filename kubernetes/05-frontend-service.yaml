apiVersion: v1
kind: Service
metadata:
  name: eck-ui-frontend
  namespace: eck-ui
  labels:
    app: eck-ui
    component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: eck-ui
    component: frontend
